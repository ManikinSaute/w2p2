<?php

/*

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}


function w2p2_log_bridge( string $message, string $level = 'INFO' ): void {
	if ( function_exists( 'pdf2p2_log' ) ) {
		pdf2p2_log( $message, $level );
	} else {
		// Fallback if your logger isn't available for some reason.
		// phpcs:ignore WordPress.PHP.DevelopmentFunctions.error_log_error_log
		error_log( sprintf( '[%s] %s', strtoupper( $level ), $message ) );
	}
}


*/
